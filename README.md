# Stripe Coupons Generator

> Quick script to generate one coupon with a bunch of one time usable promotion codes

```sh
npm install

# edit the settings of index.js and create a .env from .env.dist
npm start
```
